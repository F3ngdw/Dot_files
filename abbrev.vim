iabbrev teh the
iabbrev tehn then
iabbrev waht what
iabbrev adn and
iabbrev bindj httpCode, errCode, errMsg := app.BindJsonAndValid(c, &)<cr>if errCode != e.SUCCESS {<cr>appG.Response(httpCode, errCode, nil, errMsg)<cr>return}<esc>
iabbrev bindq httpCode, errCode, errMsg := app.BindQueryAndValid(c, &)<cr>if errCode != e.SUCCESS {<cr>appG.Response(httpCode, errCode, nil, errMsg)<cr>return}<esc>
